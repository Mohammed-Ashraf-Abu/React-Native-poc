import { StyleSheet } from "react-native";


export const segmentStyle = StyleSheet.create({
  container: {
    alignItems: 'center',
    borderColor:"black",
    // borderWidth:1
    marginBottom:10
  },
});